import { useState } from "react";
import { LabRatings, MultipleChoiceRunner, scoreMultipleChoice, StudyPanel, TextResponse, useExperimentTimer } from "../engines/shared";
import type { DoodleEffectEngineData, LabEngineProps } from "../methodEngineTypes";
type Stage = "intro" | "study" | "doodle" | "reconstruct" | "test" | "reflection";
type Props = LabEngineProps<DoodleEffectEngineData>;
function DoodleEffectExperiment({ method, category, name, data, onComplete }: Props) {
  const [stage, setStage] = useState<Stage>("intro"); const [doodle, setDoodle] = useState(""); const [reconstruction, setReconstruction] = useState(""); const [answers, setAnswers] = useState<Record<string, string>>({}); const [index, setIndex] = useState(0); const [confidence, setConfidence] = useState<number | null>(null); const [ease, setEase] = useState<number | null>(null); const [willingnessToUse, setWillingnessToUse] = useState<number | null>(null); const { start, elapsedMs } = useExperimentTimer(); const current = data.questions[index];
  function finish() { if (confidence === null || ease === null || willingnessToUse === null) return; const { correct, total, score } = scoreMultipleChoice(data.questions, answers); console.log("DOODLE:", { doodle, reconstruction }); onComplete({ method, category, correct, total, score, confidence, ease, willingnessToUse, timeSpentMs: elapsedMs() }); }
  return <section className="method-experiment">
    {stage === "intro" && <div><p>METHOD LAB · UNDERSTANDING</p><h2>{name}</h2><p>You'll encode important ideas and relationships in a simple doodle, then use it to reconstruct the material.</p><button type="button" onClick={() => { start(); setStage("study"); }}>Start experiment</button></div>}
    {stage === "study" && <div><p>STEP 1 · STUDY</p><h2>{data.topic}</h2><StudyPanel><p>{data.material}</p><ul>{data.relationships.map((item) => <li key={item}>{item}</li>)}</ul></StudyPanel><button type="button" onClick={() => setStage("doodle")}>Create a doodle</button></div>}
    {stage === "doodle" && <div><p>STEP 2 · VISUALIZE</p><h2>Describe a simple doodle of the important relationships.</h2><TextResponse value={doodle} onChange={setDoodle} placeholder="Describe your doodle and connections..."/><button type="button" disabled={doodle.trim().length < 20} onClick={() => setStage("reconstruct")}>Use my doodle</button></div>}
    {stage === "reconstruct" && <div><p>STEP 3 · RECONSTRUCT</p><h2>Use your visual idea to reconstruct or explain the material.</h2><TextResponse value={reconstruction} onChange={setReconstruction} placeholder="Reconstruct the material..."/><button type="button" disabled={reconstruction.trim().length < 20} onClick={() => setStage("test")}>Continue to test</button></div>}
    {stage === "test" && <div><p>STEP 4 · FINAL TEST</p><p>Question {index + 1} / {data.questions.length}</p><MultipleChoiceRunner question={current} selectedAnswer={answers[current.id]} onSelect={(option) => setAnswers((previous) => ({ ...previous, [current.id]: option }))} onNext={() => index < data.questions.length - 1 ? setIndex((value) => value + 1) : setStage("reflection")} nextLabel={index === data.questions.length - 1 ? "Finish test" : "Next question"}/></div>}
    {stage === "reflection" && <div><p>STEP 5 · REFLECT</p><h2>How did {name} feel?</h2><LabRatings confidence={{ prompt: "How confident were you?", value: confidence, onChange: setConfidence }} ease={{ prompt: "How easy was the method?", value: ease, onChange: setEase }} willingnessToUse={{ prompt: <>Would you use {name} while studying?</>, value: willingnessToUse, onChange: setWillingnessToUse }} completeLabel="Complete experiment" onComplete={finish}/></div>}
  </section>;
}
export default DoodleEffectExperiment;
