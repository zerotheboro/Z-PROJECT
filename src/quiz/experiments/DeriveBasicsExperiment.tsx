import { useState } from "react";
import { LabRatings, MultipleChoiceRunner, scoreMultipleChoice, StudyPanel, TextResponse, useExperimentTimer } from "../engines/shared";
import type { DeriveBasicsEngineData, LabEngineProps } from "../methodEngineTypes";
type Stage = "intro" | "inspect" | "basics" | "derive" | "chain" | "test" | "reflection";
type Props = LabEngineProps<DeriveBasicsEngineData>;
function DeriveBasicsExperiment({ method, category, name, data, onComplete }: Props) {
  const [stage, setStage] = useState<Stage>("intro"); const [basics, setBasics] = useState(""); const [derivation, setDerivation] = useState(""); const [chain, setChain] = useState(""); const [answers, setAnswers] = useState<Record<string, string>>({}); const [index, setIndex] = useState(0); const [confidence, setConfidence] = useState<number | null>(null); const [ease, setEase] = useState<number | null>(null); const [willingnessToUse, setWillingnessToUse] = useState<number | null>(null); const { start, elapsedMs } = useExperimentTimer(); const current = data.questions[index];
  function finish() { if (confidence === null || ease === null || willingnessToUse === null) return; const { correct, total, score } = scoreMultipleChoice(data.questions, answers); console.log("DERIVE BASICS:", { basics, derivation, chain }); onComplete({ method, category, correct, total, score, confidence, ease, willingnessToUse, timeSpentMs: elapsedMs() }); }
  return <section className="method-experiment">
    {stage === "intro" && <div><p>METHOD LAB · UNDERSTANDING</p><h2>{name}</h2><p>You'll identify the basic principles beneath an idea and derive the higher-level conclusion.</p><button type="button" onClick={() => { start(); setStage("inspect"); }}>Start experiment</button></div>}
    {stage === "inspect" && <div><p>STEP 1 · INSPECT</p><h2>{data.topic}</h2><p>{data.prompt}</p><StudyPanel><p>{data.source}</p></StudyPanel><button type="button" onClick={() => setStage("basics")}>Identify the basics</button></div>}
    {stage === "basics" && <div><p>STEP 2 · BASIC PRINCIPLES</p><h2>What are the simplest facts underneath the idea?</h2><TextResponse value={basics} onChange={setBasics} placeholder="Identify the basic principles..."/><button type="button" disabled={basics.trim().length < 25} onClick={() => setStage("derive")}>Derive the idea</button></div>}
    {stage === "derive" && <div><p>STEP 3 · DERIVE</p><StudyPanel><ul>{data.basicPrinciples.map((item) => <li key={item}>{item}</li>)}</ul></StudyPanel><TextResponse value={derivation} onChange={setDerivation} placeholder="Derive the higher-level idea..."/><button type="button" disabled={derivation.trim().length < 25} onClick={() => setStage("chain")}>Explain the reasoning chain</button></div>}
    {stage === "chain" && <div><p>STEP 4 · REASONING CHAIN</p><h2>Connect each basic principle to the conclusion.</h2><TextResponse value={chain} onChange={setChain} placeholder="Explain the complete reasoning chain..."/><button type="button" disabled={chain.trim().length < 25} onClick={() => setStage("test")}>Continue to test</button></div>}
    {stage === "test" && <div><p>STEP 5 · APPLICATION TEST</p><p>Question {index + 1} / {data.questions.length}</p><MultipleChoiceRunner question={current} selectedAnswer={answers[current.id]} onSelect={(option) => setAnswers((previous) => ({ ...previous, [current.id]: option }))} onNext={() => index < data.questions.length - 1 ? setIndex((value) => value + 1) : setStage("reflection")} nextLabel={index === data.questions.length - 1 ? "Finish test" : "Next question"}/></div>}
    {stage === "reflection" && <div><p>STEP 6 · REFLECT</p><h2>How did {name} feel?</h2><LabRatings confidence={{ prompt: "How confident were you?", value: confidence, onChange: setConfidence }} ease={{ prompt: "How easy was the method?", value: ease, onChange: setEase }} willingnessToUse={{ prompt: <>Would you use {name} while studying?</>, value: willingnessToUse, onChange: setWillingnessToUse }} completeLabel="Complete experiment" onComplete={finish}/></div>}
  </section>;
}
export default DeriveBasicsExperiment;
