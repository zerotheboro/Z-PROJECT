import { useState } from "react";
import { LabRatings, MultipleChoiceRunner, scoreMultipleChoice, StudyPanel, TextResponse, useExperimentTimer } from "../engines/shared";
import type { EightyTwentyEngineData, LabEngineProps } from "../methodEngineTypes";
type Stage = "intro" | "inspect" | "identify" | "prioritize" | "study" | "test" | "reflection";
type Props = LabEngineProps<EightyTwentyEngineData>;
function EightyTwentyExperiment({ method, category, name, data, onComplete }: Props) {
  const [stage, setStage] = useState<Stage>("intro"); const [highValue, setHighValue] = useState(""); const [priority, setPriority] = useState(""); const [answers, setAnswers] = useState<Record<string, string>>({}); const [index, setIndex] = useState(0); const [confidence, setConfidence] = useState<number | null>(null); const [ease, setEase] = useState<number | null>(null); const [willingnessToUse, setWillingnessToUse] = useState<number | null>(null); const { start, elapsedMs } = useExperimentTimer(); const current = data.questions[index];
  function finish() { if (confidence === null || ease === null || willingnessToUse === null) return; const { correct, total, score } = scoreMultipleChoice(data.questions, answers); console.log("80/20:", { highValue, priority }); onComplete({ method, category, correct, total, score, confidence, ease, willingnessToUse, timeSpentMs: elapsedMs() }); }
  return <section className="method-experiment">
    {stage === "intro" && <div><p>METHOD LAB · ORGANIZATION</p><h2>{name}</h2><p>You'll identify and prioritize the few concepts that produce most of the value.</p><button type="button" onClick={() => { start(); setStage("inspect"); }}>Start experiment</button></div>}
    {stage === "inspect" && <div><p>STEP 1 · INSPECT</p><h2>{data.topic}</h2><StudyPanel><ul>{data.details.map((item) => <li key={item}>{item}</li>)}</ul></StudyPanel><button type="button" onClick={() => setStage("identify")}>Identify high-value concepts</button></div>}
    {stage === "identify" && <div><p>STEP 2 · IDENTIFY</p><h2>Which small set of concepts will produce most of the value?</h2><TextResponse value={highValue} onChange={setHighValue} placeholder="Identify the high-value concepts..."/><button type="button" disabled={highValue.trim().length < 20} onClick={() => setStage("prioritize")}>Prioritize them</button></div>}
    {stage === "prioritize" && <div><p>STEP 3 · PRIORITIZE</p><h2>Put your highest-value concepts in priority order.</h2><TextResponse value={priority} onChange={setPriority} placeholder="Rank the concepts and explain your order..."/><button type="button" disabled={priority.trim().length < 20} onClick={() => setStage("study")}>Study the priorities</button></div>}
    {stage === "study" && <div><p>STEP 4 · STUDY</p><h2>High-value concepts</h2><StudyPanel><ol>{data.coreConcepts.map((item) => <li key={item}>{item}</li>)}</ol></StudyPanel><button type="button" onClick={() => setStage("test")}>Continue to test</button></div>}
    {stage === "test" && <div><p>STEP 5 · APPLICATION TEST</p><p>Question {index + 1} / {data.questions.length}</p><MultipleChoiceRunner question={current} selectedAnswer={answers[current.id]} onSelect={(option) => setAnswers((previous) => ({ ...previous, [current.id]: option }))} onNext={() => index < data.questions.length - 1 ? setIndex((value) => value + 1) : setStage("reflection")} nextLabel={index === data.questions.length - 1 ? "Finish test" : "Next question"}/></div>}
    {stage === "reflection" && <div><p>STEP 6 · REFLECT</p><h2>How did {name} feel?</h2><LabRatings confidence={{ prompt: "How confident were you?", value: confidence, onChange: setConfidence }} ease={{ prompt: "How easy was the method?", value: ease, onChange: setEase }} willingnessToUse={{ prompt: <>Would you use {name} while studying?</>, value: willingnessToUse, onChange: setWillingnessToUse }} completeLabel="Complete experiment" onComplete={finish}/></div>}
  </section>;
}
export default EightyTwentyExperiment;
