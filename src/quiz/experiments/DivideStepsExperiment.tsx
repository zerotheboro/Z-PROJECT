import { useState } from "react";
import { LabRatings, MultipleChoiceRunner, scoreMultipleChoice, StudyPanel, TextResponse, useExperimentTimer } from "../engines/shared";
import type { DivideStepsEngineData, LabEngineProps } from "../methodEngineTypes";
type Stage = "intro" | "inspect" | "divide" | "organize" | "reconstruct" | "test" | "reflection";
type Props = LabEngineProps<DivideStepsEngineData>;
function DivideStepsExperiment({ method, category, name, data, onComplete }: Props) {
  const [stage, setStage] = useState<Stage>("intro"); const [steps, setSteps] = useState(""); const [sequence, setSequence] = useState(""); const [explanation, setExplanation] = useState(""); const [answers, setAnswers] = useState<Record<string, string>>({}); const [index, setIndex] = useState(0); const [confidence, setConfidence] = useState<number | null>(null); const [ease, setEase] = useState<number | null>(null); const [willingnessToUse, setWillingnessToUse] = useState<number | null>(null); const { start, elapsedMs } = useExperimentTimer(); const current = data.questions[index];
  function finish() { if (confidence === null || ease === null || willingnessToUse === null) return; const { correct, total, score } = scoreMultipleChoice(data.questions, answers); console.log("DIVIDE STEPS:", { steps, sequence, explanation }); onComplete({ method, category, correct, total, score, confidence, ease, willingnessToUse, timeSpentMs: elapsedMs() }); }
  return <section className="method-experiment">
    {stage === "intro" && <div><p>METHOD LAB · ORGANIZATION</p><h2>{name}</h2><p>You'll divide a complex process into smaller steps, organize them, and reconstruct the whole process.</p><button type="button" onClick={() => { start(); setStage("inspect"); }}>Start experiment</button></div>}
    {stage === "inspect" && <div><p>STEP 1 · INSPECT THE PROCESS</p><h2>{data.topic}</h2><StudyPanel><p>{data.overview}</p></StudyPanel><button type="button" onClick={() => setStage("divide")}>Divide the process</button></div>}
    {stage === "divide" && <div><p>STEP 2 · DIVIDE</p><h2>Break the process into clear smaller steps.</h2><TextResponse value={steps} onChange={setSteps} placeholder="List the smaller steps..."/><button type="button" disabled={steps.trim().length < 25} onClick={() => setStage("organize")}>Organize the sequence</button></div>}
    {stage === "organize" && <div><p>STEP 3 · ORGANIZE</p><h2>Put the steps in a clear sequence.</h2><TextResponse value={sequence} onChange={setSequence} placeholder="Organize the steps in order..."/><button type="button" disabled={sequence.trim().length < 25} onClick={() => setStage("reconstruct")}>Reconstruct the process</button></div>}
    {stage === "reconstruct" && <div><p>STEP 4 · RECONSTRUCT</p><StudyPanel><ol>{data.referenceSteps.map((item) => <li key={item}>{item}</li>)}</ol></StudyPanel><TextResponse value={explanation} onChange={setExplanation} placeholder="Explain how the complete process works..."/><button type="button" disabled={explanation.trim().length < 25} onClick={() => setStage("test")}>Continue to test</button></div>}
    {stage === "test" && <div><p>STEP 5 · PROCESS TEST</p><p>Question {index + 1} / {data.questions.length}</p><MultipleChoiceRunner question={current} selectedAnswer={answers[current.id]} onSelect={(option) => setAnswers((previous) => ({ ...previous, [current.id]: option }))} onNext={() => index < data.questions.length - 1 ? setIndex((value) => value + 1) : setStage("reflection")} nextLabel={index === data.questions.length - 1 ? "Finish test" : "Next question"}/></div>}
    {stage === "reflection" && <div><p>STEP 6 · REFLECT</p><h2>How did {name} feel?</h2><LabRatings confidence={{ prompt: "How confident were you?", value: confidence, onChange: setConfidence }} ease={{ prompt: "How easy was the method?", value: ease, onChange: setEase }} willingnessToUse={{ prompt: <>Would you use {name} while studying?</>, value: willingnessToUse, onChange: setWillingnessToUse }} completeLabel="Complete experiment" onComplete={finish}/></div>}
  </section>;
}
export default DivideStepsExperiment;
